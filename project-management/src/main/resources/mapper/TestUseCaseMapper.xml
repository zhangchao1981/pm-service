<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iscas.pm.api.mapper.test.TestUseCaseMapper">

    <sql id="Base_Column_List">
        test_use_case.*,dev_modular.name as modularName
    </sql>


    <select id="testUseCaseListForPlan" resultType="com.iscas.pm.api.model.test.TestUseCase">
        SELECT <include refid="Base_Column_List" />
        FROM test_use_case ,dev_modular
        WHERE test_use_case.module_id=dev_modular.id
        <if test="param.creatorId !=null">
            and test_use_case.creatorId=#{param.creatorId}
        </if>
        <if test="param.useCaseType !=null and param.useCaseType != ''">
            and type =#{param.useCaseType}
        </if>
        <if test="param.currentProcessor !=null and param.currentProcessor != ''">
            and current_processor=#{param.currentProcessor}
        </if>
        <if test="param.modularList !=null and param.modularList.size()>0 ">
            and modular_id in
            <foreach item="modularId" index="index" collection="modularList"
                     open="(" separator="," close=")">
                #{param.modularList}
            </foreach>
        </if>


        <if test="param.titleOrId !=null ">
            and (title = #{param.titleOrId} or convert(id,char)=  #{param.titleOrId})
        </if>
    </select>

</mapper>
